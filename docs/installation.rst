============
Installation
============

At the command line::

    $ easy_install django-analystnotes

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-analystnotes
    $ pip install django-analystnotes
